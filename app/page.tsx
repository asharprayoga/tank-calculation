export default function Home() {
  return (
    <main className="min-h-screen p-10 bg-blue-600 text-white">
      <h1 className="text-4xl font-bold">TAILWIND OK</h1>
      <p className="mt-2">Kalau halaman ini biru, Tailwind sudah hidup.</p>
    </main>
  );
}            <span className="h-2 w-2 rounded-full bg-[rgb(var(--re-green))]" />
            <span className="h-2 w-2 rounded-full bg-[rgb(var(--re-orange))]" />
            Brand colors applied
          </div>
        </section>
      </div>
    </main>
  );
}
